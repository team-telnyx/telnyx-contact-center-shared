---
title: Reject Call
description: Reject an inbound call without answering it.
---

**Reject Call** declines an inbound call using the configured reason/cause. Use it for policy, schedule, capacity, or validation failures that should not be answered.

Choose the rejection reason deliberately because carriers and calling clients may present it differently. Log the business reason without exposing sensitive caller data.
