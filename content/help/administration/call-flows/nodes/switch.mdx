---
title: Switch
description: Route one value across several cases and a default path.
---

**Switch** evaluates a value/expression and selects the matching case output. Use it when several mutually exclusive outcomes would make chained conditions hard to read.

Keep cases unique, normalize case/value types, and always connect a default/unmatched branch. Do not rely on visual case order to resolve duplicate matches.
