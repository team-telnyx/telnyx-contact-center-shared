---
title: Start AI Assistant
description: Attach a configured AI assistant to the call.
---

This node starts the selected assistant with supported instructions/context and call settings. Pass only the runtime context the assistant needs.

Handle assistant start failure, tool failure, timeout, and caller hangup. Define a human/fallback route and pair successful starts with Stop AI Assistant when the flow regains control.
