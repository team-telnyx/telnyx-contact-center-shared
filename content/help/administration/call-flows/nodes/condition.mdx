---
title: Condition
description: Route execution from one comparison.
---

**Condition** compares left and right operands using the selected operator and data type, then emits true/false branches.

Supported operators include equality/inequality, numeric ordering, contains, startsWith, and endsWith. Use variables or expressions, normalize types explicitly, and connect both outcomes. Test missing, empty, zero, and `false` separately.
