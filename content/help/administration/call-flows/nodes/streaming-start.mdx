---
title: Streaming Start
description: Start streaming call media to an external endpoint.
---

This node streams selected call media according to the configured URL, track, and protocol options.

Authenticate the receiver, verify TLS/reachability, bound reconnect behavior, and handle receiver failure without trapping the caller. Pair with Streaming Stop and review the external system's data retention.
