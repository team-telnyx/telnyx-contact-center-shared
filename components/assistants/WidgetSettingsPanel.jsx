"use client";

import { useState } from "react";
import { IconCheck, IconCopy } from "@tabler/icons-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Switch } from "@/components/ui/switch";

export default function WidgetSettingsPanel({ values, setValues, assistantId }) {
  const [copied, setCopied] = useState(false);
  const settings = values.widget_settings || {};
  const enabled = Boolean(values.telephony?.supports_unauthenticated_web_calls);
  function patch(next) { setValues((current) => ({ ...current, widget_settings: { ...(current.widget_settings || {}), ...next } })); }
  function toggle(checked) { setValues((current) => ({ ...current, telephony: { ...(current.telephony || {}), supports_unauthenticated_web_calls: checked } })); }
  const snippet = `<telnyx-ai-agent agent-id="${assistantId || "YOUR_ASSISTANT_ID"}"></telnyx-ai-agent>`;
  async function copy() { await navigator.clipboard.writeText(snippet); setCopied(true); window.setTimeout(() => setCopied(false), 1400); }
  return <div className="space-y-4"><Card><CardHeader><CardTitle>Web calling widget</CardTitle><CardDescription>Configure the embedded browser experience for this assistant.</CardDescription></CardHeader><CardContent className="space-y-5"><div className="flex items-center justify-between rounded-lg border p-4"><div><div className="font-medium">Enable unauthenticated web calls</div><div className="text-xs text-muted-foreground">Allows visitors to start a browser call through the widget.</div></div><Switch checked={enabled} onCheckedChange={toggle} /></div>{enabled ? <><div className="grid gap-4 md:grid-cols-3"><div className="space-y-2"><Label>Theme</Label><Select value={settings.theme || "dark"} onValueChange={(value) => patch({ theme: value })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="dark">Dark</SelectItem><SelectItem value="light">Light</SelectItem><SelectItem value="system">System</SelectItem></SelectContent></Select></div><div className="space-y-2"><Label>Default state</Label><Select value={settings.default_state || "collapsed"} onValueChange={(value) => patch({ default_state: value })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="collapsed">Collapsed</SelectItem><SelectItem value="expanded">Expanded</SelectItem></SelectContent></Select></div><div className="space-y-2"><Label>Position</Label><Select value={settings.position || "fixed"} onValueChange={(value) => patch({ position: value })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="fixed">Fixed</SelectItem><SelectItem value="inline">Inline</SelectItem></SelectContent></Select></div></div><div className="grid gap-4 md:grid-cols-2"><div className="space-y-2"><Label>Start call text</Label><Input value={settings.start_call_text || ""} onChange={(event) => patch({ start_call_text: event.target.value || undefined })} placeholder="Start a call" /></div><div className="space-y-2"><Label>Agent thinking text</Label><Input value={settings.agent_thinking_text || ""} onChange={(event) => patch({ agent_thinking_text: event.target.value || undefined })} placeholder="Thinking…" /></div><div className="space-y-2"><Label>Speak to interrupt text</Label><Input value={settings.speak_to_interrupt_text || ""} onChange={(event) => patch({ speak_to_interrupt_text: event.target.value || undefined })} placeholder="Speak to interrupt" /></div><div className="space-y-2"><Label>Logo URL</Label><Input type="url" value={settings.logo_icon_url || ""} onChange={(event) => patch({ logo_icon_url: event.target.value || null })} /></div></div><div className="grid gap-4 md:grid-cols-2"><div className="space-y-2"><Label>Visualizer color</Label><Select value={settings.audio_visualizer_config?.color || "verdant"} onValueChange={(value) => patch({ audio_visualizer_config: { ...(settings.audio_visualizer_config || {}), color: value } })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="verdant">Verdant</SelectItem><SelectItem value="blue">Blue</SelectItem><SelectItem value="violet">Violet</SelectItem><SelectItem value="amber">Amber</SelectItem></SelectContent></Select></div><div className="space-y-2"><Label>Visualizer preset</Label><Select value={settings.audio_visualizer_config?.preset || "round_bars"} onValueChange={(value) => patch({ audio_visualizer_config: { ...(settings.audio_visualizer_config || {}), preset: value } })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="round_bars">Round bars</SelectItem><SelectItem value="wave">Wave</SelectItem><SelectItem value="bars">Bars</SelectItem></SelectContent></Select></div></div></> : null}</CardContent></Card>{enabled ? <Card><CardHeader><CardTitle>Embed code</CardTitle><CardDescription>Add the Telnyx AI Agent web component to your site.</CardDescription></CardHeader><CardContent><div className="flex items-center gap-3 rounded-lg border bg-zinc-950 p-4 text-zinc-100"><code className="min-w-0 flex-1 overflow-x-auto text-xs">{snippet}</code><Button variant="secondary" size="sm" onClick={copy}>{copied ? <IconCheck className="size-4" /> : <IconCopy className="size-4" />}{copied ? "Copied" : "Copy"}</Button></div></CardContent></Card> : null}</div>;
}
